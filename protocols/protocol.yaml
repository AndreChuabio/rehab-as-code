# Andre, 26 — post-ACL reconstruction ~3 weeks post-op; intake pain 3/10, mild at ~110° flexion.
# Week 3 library baseline; evaluator HOLD — no week-4 advancement; −20% session intensity (sleep rule).
# Cursor cloud agents update this via PR per .cursorrules.

patient: Andre
phase: post-ACL reconstruction
week: 3
generated_by: "Coordinator 2026-05-04: Researcher PRIMARY protocol-library/post-acl-week-3.yaml (week 3 baseline; knee/post-acl-week-4.yaml reserved until hold clears); Evaluator HOLD — recovery_7day_avg 35 below 60 (no progression clearance); −20% session intensity per sleep_score_7day_avg rule (<70); acute snapshot sleep 93, recovery 70, HRV 56 improving (delta 7d = 0, no +5 ms single-exercise clearance)."
last_updated: "2026-05-04"

session_targets:
  frequency_per_week: 3
  duration_min: 24
  max_pain_during_session: 3

exercises:
  - name: stationary_bike
    duration_min: 8
    intensity: "RPE 3 of 10"
    progression_criteria: "when sleep/recovery thresholds clear — advance toward library 10–12 min and RPE 4/10"
    regression_criteria: "patellofemoral pain mid-session"
    references:
      - protocol-library/post-acl-week-3.yaml

  - name: terminal_knee_extension
    sets: 3
    reps: 10
    load: yellow_band
    ROM_target_deg: 0
    progression_criteria: "matches contralateral extension before adding volume"
    regression_criteria: "extension lag > 5 deg"
    references:
      - protocol-library/post-acl-week-3.yaml

  - name: quad_sets
    sets: 3
    reps: 12
    load: bodyweight
    ROM_target_deg: 90
    progression_criteria: "pain-free on current reps; no swelling next morning before returning toward 15 reps"
    regression_criteria: "any swelling within 24h"
    references:
      - protocol-library/post-acl-week-3.yaml

  - name: heel_slides
    sets: 3
    reps: 8
    ROM_target_deg: 100
    progression_criteria: "pain-free sustained 100° active flexion (library milestone); defer higher ROM until hold clears/clinician ok"
    regression_criteria: "ROM regression > 5 deg week-over-week"
    references:
      - protocol-library/post-acl-week-3.yaml
